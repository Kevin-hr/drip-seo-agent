# V5.1 LLM Evidence Layer — Superseded

The historical `feature/v5-llm-evidence-layer` branch is not an active merge candidate.
Its useful principles—evidence tiers, deterministic PASS/HOLD decisions, and refusal
to let an LLM self-certify—are implemented by `seo-core/`, Agent Contract V2.0, and
the V4.4 bridge on `main`.

Do not merge the historical plan into `main`. Preserve the branch only as design
provenance until routine branch cleanup removes it.
